import { NextRequest, NextResponse } from "next/server";
import { checkR2Health, saveJsonToR2 } from "@/lib/r2-warehouse";
const sources=[ ["fmp","stocks"], ["fmp","crypto"], ["sec","tickers"], ["coingecko","coins"], ["frankfurter","currencies"], ["fred","series-registry"], ["marketaux","entities"], ["alpha-vantage","stocks"] ];
export async function POST(request:NextRequest){ const body=await request.json().catch(()=>({})); const dryRun=body?.dryRun !== false; const date=new Date().toISOString().slice(0,10); const r2=await checkR2Health(false); const planned=sources.map(([source,assetType])=>({ source, assetType, r2Key:`universe/${source}/${assetType}/${date}.json` })); const saved=[] as string[]; if(!dryRun && r2.connected){ for(const item of planned){ await saveJsonToR2(item.r2Key, { source:item.source, assetType:item.assetType, syncedAt:new Date().toISOString(), records:[] }, { source:item.source, assetType:item.assetType, dataType:"universe", recordCount:0 }); saved.push(item.r2Key); } } return NextResponse.json({ ok:true, dryRun, r2Connected:r2.connected, rawWarehouseMode:r2.connected?"r2":"postgresql-only-fallback", planned, saved }); }
