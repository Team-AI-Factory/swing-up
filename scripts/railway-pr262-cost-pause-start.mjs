console.log("[swing-up-cost-control] PR #262 Railway compute is HARD PAUSED during consolidation. Exiting successfully without starting a web server, sensor, worker, or legacy supervisor.");
process.exit(0);
